namespace Gameframe.GUI.PanelSystem
{
    /// <summary>
    /// When there is a transition the panel controller will receive an ITransitionEvent with details about the transition
    /// In some cases a panel may require details about what is being shown and what is being hidden to transition properly
    /// Those details should be contained in an object that implements this interface
    /// </summary>
    public interface ITransitionEvent
    {                                                                                                                                                                                                             
    }
}